import React from 'react';
import styles from './RegisterBtn.module.css';

const RegisterBtn = () => {
	return <button className={styles.register_button}>Register</button>;
};

export default RegisterBtn;
